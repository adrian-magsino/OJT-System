

export default async function HteProfilePage({ params }) {
  const { id } = await params;

  return (
    <div>
      {/*Back button for mobile*/}
      <div>BACK BUTTON FOR MOBILE</div>

      <div className="p-4">
        {/*DISPLAY HTE PROFILE COMPONENT HERE */}
      </div>
    </div>
  );
}